services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./work:/home/jovyan/work
    command: >
      start-notebook.sh
      --ServerApp.token=''
      --ServerApp.password=''
